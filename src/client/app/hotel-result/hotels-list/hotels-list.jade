ul.container-fluid.hotel-container
    li(ng-repeat='hotel in $ctrl.hotels |filterPrice:($ctrl.filters.price)|filterName:($ctrl.filters.name) track by $index')
        hotel-item(item='hotel')
        
        
        
